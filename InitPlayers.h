//Initializes the players to the battle.

#pragma once
#include "Character.h"

namespace InitPlayers{

    //Set the spacing so the enemies are drawn correctly on the screen.
    void initPlayersSpacing(vector<Character*> thePlayers);
}